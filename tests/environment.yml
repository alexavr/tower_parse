name: tower_client
dependencies:
  - python>=3.6
  - numpy
  - regex  # optional, enables advanced regex
  - pytest  # only for development & testing
  - pip
  - pip:
      - black  # code formatting during development
